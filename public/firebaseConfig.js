export const firebaseConfig = {
  // Paste your configuration here from Firebase console -> Project Settings -> general -> web app
  // To prevent accidental commit/push of this file run: git update-index --skip-worktree public/firebaseConfig.js
}

// Paste your messaging vapid key here from Firebase console -> Project Settings -> Cloud Messaging -> Key Pair
export const vapidKey = ''
